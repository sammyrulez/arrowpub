<%= form_for([@shooting_range, @shooting_session]) do |f| %>
  <% if @shooting_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shooting_session.errors.count, "error") %> prohibited this shooting_session from being saved:</h2>

      <ul>
      <% @shooting_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :performed_on %><br />
    <%= f.date_select :performed_on %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
